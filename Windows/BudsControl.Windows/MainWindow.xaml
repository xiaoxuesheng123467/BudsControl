<Window x:Class="BudsControl.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BudsControl.Windows"
        Title="BudsControl for Windows"
        Width="1120" Height="800" MinWidth="920" MinHeight="640"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded" Closed="Window_Closed">
  <Window.Resources>
    <local:ModeEqualsConverter x:Key="ModeEqualsConverter" />
    <Style x:Key="ChoiceToggle" TargetType="ToggleButton">
      <Setter Property="MinWidth" Value="94" />
      <Setter Property="Height" Value="40" />
      <Setter Property="Margin" Value="0,0,8,0" />
      <Setter Property="Padding" Value="12,5" />
      <Setter Property="Background" Value="#F5F7F6" />
      <Setter Property="BorderBrush" Value="{StaticResource LineBrush}" />
      <Setter Property="Cursor" Value="Hand" />
      <Style.Triggers>
        <Trigger Property="IsChecked" Value="True">
          <Setter Property="Background" Value="#DDEFE3" />
          <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
          <Setter Property="Foreground" Value="{StaticResource AccentDarkBrush}" />
          <Setter Property="FontWeight" Value="SemiBold" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="NavTab" TargetType="TabItem">
      <Setter Property="Width" Value="190" />
      <Setter Property="Height" Value="48" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="Padding" Value="16,0" />
      <Setter Property="Margin" Value="8,2" />
      <Setter Property="FontSize" Value="14" />
      <Style.Triggers>
        <Trigger Property="IsSelected" Value="True">
          <Setter Property="Foreground" Value="{StaticResource AccentDarkBrush}" />
          <Setter Property="FontWeight" Value="SemiBold" />
        </Trigger>
      </Style.Triggers>
    </Style>
    <Style x:Key="InlineLabel" TargetType="TextBlock" BasedOn="{StaticResource FieldLabel}">
      <Setter Property="Width" Value="180" />
      <Setter Property="Margin" Value="0,0,16,0" />
    </Style>
    <Style x:Key="SettingCheck" TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
      <Setter Property="Margin" Value="0,0,0,7" />
    </Style>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="76" />
      <RowDefinition Height="*" />
      <RowDefinition Height="40" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,0,0,1">
      <Grid Margin="24,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <Border Width="40" Height="40" CornerRadius="6" Background="{StaticResource AccentDarkBrush}" VerticalAlignment="Center">
          <TextBlock Text="B" Foreground="White" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <StackPanel Grid.Column="1" Margin="14,0,0,0" VerticalAlignment="Center">
          <TextBlock Text="BudsControl" FontSize="20" FontWeight="Bold" />
          <TextBlock Text="没有三星手机，也能扩展 Galaxy Buds 功能" Foreground="{StaticResource MutedBrush}" FontSize="12" />
        </StackPanel>
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
          <Border CornerRadius="6" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="10,5" Background="#F7F9F8">
            <TextBlock Text="{Binding ConnectionStateText}" FontWeight="SemiBold" />
          </Border>
          <Button Style="{StaticResource IconButton}" Margin="10,0,0,0" Command="{Binding RefreshDevicesCommand}" ToolTip="刷新已配对设备" Content="&#xE72C;" />
        </StackPanel>
      </Grid>
    </Border>

    <TabControl Grid.Row="1" TabStripPlacement="Left" Background="{StaticResource PageBrush}" BorderThickness="0" Padding="0">
      <TabControl.Resources>
        <Style TargetType="TabItem" BasedOn="{StaticResource NavTab}" />
      </TabControl.Resources>

      <TabItem Header="总览">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <Grid MaxWidth="900" HorizontalAlignment="Center" Margin="28,26,28,40">
            <StackPanel>
              <TextBlock Text="Galaxy Buds3 Pro" FontSize="30" FontWeight="Bold" />
              <TextBlock Text="Windows 原生直连控制，无需三星电脑、账号或云服务" FontSize="15" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,18" />

              <Border Style="{StaticResource SectionBorder}">
                <StackPanel>
                  <TextBlock Text="直连耳机" Style="{StaticResource SectionTitle}" />
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ComboBox ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}" MinWidth="360" ToolTip="仅显示已在 Windows 设置中配对的 Galaxy Buds" />
                    <Button Grid.Column="1" Content="连接" Style="{StaticResource PrimaryButton}" Command="{Binding ConnectCommand}" Margin="12,0,8,0" />
                    <Button Grid.Column="2" Content="断开" Command="{Binding DisconnectCommand}" Margin="0" />
                  </Grid>
                  <TextBlock Text="{Binding ConnectionDetail}" Foreground="{StaticResource MutedBrush}" Margin="0,10,0,0" />
                </StackPanel>
              </Border>

              <Border Style="{StaticResource SectionBorder}">
                <StackPanel>
                  <TextBlock Text="电量" Style="{StaticResource SectionTitle}" />
                  <UniformGrid Columns="3">
                    <StackPanel><TextBlock Text="左耳" Foreground="{StaticResource MutedBrush}" /><TextBlock Text="{Binding LeftBattery}" FontSize="25" FontWeight="SemiBold" /></StackPanel>
                    <StackPanel><TextBlock Text="右耳" Foreground="{StaticResource MutedBrush}" /><TextBlock Text="{Binding RightBattery}" FontSize="25" FontWeight="SemiBold" /></StackPanel>
                    <StackPanel><TextBlock Text="充电盒" Foreground="{StaticResource MutedBrush}" /><TextBlock Text="{Binding CaseBattery}" FontSize="25" FontWeight="SemiBold" /></StackPanel>
                  </UniformGrid>
                </StackPanel>
              </Border>

              <Border Style="{StaticResource SectionBorder}">
                <StackPanel>
                  <TextBlock Text="噪音控制" Style="{StaticResource SectionTitle}" />
                  <WrapPanel>
                    <ToggleButton Style="{StaticResource ChoiceToggle}" Content="关闭" IsEnabled="{Binding CanControl}" IsChecked="{Binding NoiseMode, Converter={StaticResource ModeEqualsConverter}, ConverterParameter=0, Mode=OneWay}" Command="{Binding SetNoiseModeCommand}" CommandParameter="0" />
                    <ToggleButton Style="{StaticResource ChoiceToggle}" Content="降噪" IsEnabled="{Binding CanControl}" IsChecked="{Binding NoiseMode, Converter={StaticResource ModeEqualsConverter}, ConverterParameter=1, Mode=OneWay}" Command="{Binding SetNoiseModeCommand}" CommandParameter="1" />
                    <ToggleButton Style="{StaticResource ChoiceToggle}" Content="环境声" IsEnabled="{Binding CanControl}" IsChecked="{Binding NoiseMode, Converter={StaticResource ModeEqualsConverter}, ConverterParameter=2, Mode=OneWay}" Command="{Binding SetNoiseModeCommand}" CommandParameter="2" />
                    <ToggleButton Style="{StaticResource ChoiceToggle}" Content="自适应" IsEnabled="{Binding CanControl}" IsChecked="{Binding NoiseMode, Converter={StaticResource ModeEqualsConverter}, ConverterParameter=3, Mode=OneWay}" Command="{Binding SetNoiseModeCommand}" CommandParameter="3" />
                  </WrapPanel>
                  <TextBlock Text="Windows 直连待 Buds3 Pro 真机验证；原始 ANC / 环境声协议已由 iOS / Mac 链路确认" Style="{StaticResource VerificationText}" />
                </StackPanel>
              </Border>

              <Border Style="{StaticResource SectionBorder}">
                <StackPanel>
                  <TextBlock Text="均衡器" Style="{StaticResource SectionTitle}" />
                  <DockPanel>
                    <TextBlock Text="预设" Style="{StaticResource InlineLabel}" />
                    <ComboBox SelectedIndex="{Binding EqualizerIndex}" IsEnabled="{Binding CanControl}">
                      <ComboBoxItem Content="正常" /><ComboBoxItem Content="低音增强" /><ComboBoxItem Content="柔和" />
                      <ComboBoxItem Content="动态" /><ComboBoxItem Content="清晰" /><ComboBoxItem Content="高音增强" />
                    </ComboBox>
                  </DockPanel>
                  <TextBlock Text="六组预设协议已确认；Windows RFCOMM 直连仍待真机验证" Style="{StaticResource VerificationText}" />
                </StackPanel>
              </Border>
            </StackPanel>
          </Grid>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="环境声与自动检测">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel MaxWidth="900" HorizontalAlignment="Center" Margin="28,26,28,40">
            <TextBlock Text="环境声与自动检测" FontSize="24" FontWeight="Bold" Margin="0,0,0,18" />
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="环境声" Style="{StaticResource SectionTitle}" />
                <DockPanel Margin="0,0,0,8"><TextBlock Text="环境声级别" Style="{StaticResource InlineLabel}" /><ComboBox SelectedIndex="{Binding AmbientVolumeIndex}" IsEnabled="{Binding CanControl}"><ComboBoxItem Content="低" /><ComboBoxItem Content="中" /><ComboBoxItem Content="高" /></ComboBox></DockPanel>
                <CheckBox Content="超高环境声" IsChecked="{Binding ExtraHighAmbientEnabled}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="强降噪" IsChecked="{Binding NoiseReductionHigh}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <TextBlock Text="协议已映射，等待 Buds3 Pro 真机逐项验证" Style="{StaticResource VerificationText}" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="左右耳定制" Style="{StaticResource SectionTitle}" />
                <CheckBox Content="自定义环境声" IsChecked="{Binding AmbientCustomizationEnabled}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <Grid Margin="0,4,0,10">
                  <Grid.ColumnDefinitions><ColumnDefinition Width="180" /><ColumnDefinition Width="*" /><ColumnDefinition Width="48" /></Grid.ColumnDefinitions>
                  <TextBlock Text="左耳强度" Style="{StaticResource FieldLabel}" />
                  <Slider Grid.Column="1" Minimum="0" Maximum="2" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding AmbientVolumeLeft, Delay=350}" IsEnabled="{Binding AmbientCustomizationControlsEnabled}" />
                  <TextBlock Grid.Column="2" Text="{Binding AmbientVolumeLeft}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
                <Grid Margin="0,0,0,10">
                  <Grid.ColumnDefinitions><ColumnDefinition Width="180" /><ColumnDefinition Width="*" /><ColumnDefinition Width="48" /></Grid.ColumnDefinitions>
                  <TextBlock Text="右耳强度" Style="{StaticResource FieldLabel}" />
                  <Slider Grid.Column="1" Minimum="0" Maximum="2" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding AmbientVolumeRight, Delay=350}" IsEnabled="{Binding AmbientCustomizationControlsEnabled}" />
                  <TextBlock Grid.Column="2" Text="{Binding AmbientVolumeRight}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
                <DockPanel><TextBlock Text="音色" Style="{StaticResource InlineLabel}" /><ComboBox SelectedIndex="{Binding AmbientToneIndex}" IsEnabled="{Binding AmbientCustomizationControlsEnabled}"><ComboBoxItem Content="柔和 +2" /><ComboBoxItem Content="柔和 +1" /><ComboBoxItem Content="均衡" /><ComboBoxItem Content="清晰 +1" /><ComboBoxItem Content="清晰 +2" /></ComboBox></DockPanel>
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="自动环境声" Style="{StaticResource SectionTitle}" />
                <CheckBox Content="检测我的声音" IsChecked="{Binding VoiceDetectEnabled}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <DockPanel Margin="0,0,0,8"><TextBlock Text="恢复时间" Style="{StaticResource InlineLabel}" /><ComboBox SelectedIndex="{Binding VoiceDetectTimeoutIndex}" IsEnabled="{Binding VoiceTimeoutEnabled}"><ComboBoxItem Content="5 秒" /><ComboBoxItem Content="10 秒" /><ComboBoxItem Content="15 秒" /></ComboBox></DockPanel>
                <CheckBox Content="允许单耳使用噪音控制" IsChecked="{Binding OneEarNoiseControl}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="耳机控制">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel MaxWidth="900" HorizontalAlignment="Center" Margin="28,26,28,40">
            <TextBlock Text="耳机控制" FontSize="24" FontWeight="Bold" Margin="0,0,0,18" />
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="控制锁" Style="{StaticResource SectionTitle}" />
                <CheckBox Content="锁定耳机控制" IsChecked="{Binding TouchLocked}" IsEnabled="{Binding CanControl}" />
                <TextBlock Text="Buds3 Pro 七字节高级触控锁格式，待 Windows 真机验证" Style="{StaticResource VerificationText}" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="捏合手势" Style="{StaticResource SectionTitle}" />
                <CheckBox Content="捏一下：播放或暂停" IsChecked="{Binding SingleTapEnabled}" IsEnabled="{Binding GestureControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="捏两下：下一首" IsChecked="{Binding DoubleTapEnabled}" IsEnabled="{Binding GestureControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="捏三下：上一首" IsChecked="{Binding TripleTapEnabled}" IsEnabled="{Binding GestureControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="长捏" IsChecked="{Binding TouchAndHoldEnabled}" IsEnabled="{Binding GestureControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="捏两下接听或结束通话" IsChecked="{Binding DoubleTapCallEnabled}" IsEnabled="{Binding GestureControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="长捏拒接来电" IsChecked="{Binding TouchAndHoldCallEnabled}" IsEnabled="{Binding GestureControlsEnabled}" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="长捏动作" Style="{StaticResource SectionTitle}" />
                <DockPanel Margin="0,0,0,8"><TextBlock Text="左耳" Style="{StaticResource InlineLabel}" /><ComboBox SelectedIndex="{Binding LeftTouchActionIndex}" IsEnabled="{Binding GestureControlsEnabled}"><ComboBoxItem Content="语音助手" /><ComboBoxItem Content="切换噪声模式" /><ComboBoxItem Content="音量" /><ComboBoxItem Content="Spotify" /></ComboBox></DockPanel>
                <DockPanel Margin="0,0,0,8"><TextBlock Text="右耳" Style="{StaticResource InlineLabel}" /><ComboBox SelectedIndex="{Binding RightTouchActionIndex}" IsEnabled="{Binding GestureControlsEnabled}"><ComboBoxItem Content="语音助手" /><ComboBoxItem Content="切换噪声模式" /><ComboBoxItem Content="音量" /><ComboBoxItem Content="Spotify" /></ComboBox></DockPanel>
                <DockPanel Margin="0,0,0,8"><TextBlock Text="左耳噪音循环" Style="{StaticResource InlineLabel}" /><ComboBox SelectedIndex="{Binding LeftNoiseCycleIndex}" IsEnabled="{Binding GestureControlsEnabled}"><ComboBoxItem Content="环境声 / 关闭" /><ComboBoxItem Content="降噪 / 环境声" /><ComboBoxItem Content="降噪 / 关闭" /></ComboBox></DockPanel>
                <DockPanel Margin="0,0,0,10"><TextBlock Text="右耳噪音循环" Style="{StaticResource InlineLabel}" /><ComboBox SelectedIndex="{Binding RightNoiseCycleIndex}" IsEnabled="{Binding GestureControlsEnabled}"><ComboBoxItem Content="环境声 / 关闭" /><ComboBoxItem Content="降噪 / 环境声" /><ComboBoxItem Content="降噪 / 关闭" /></ComboBox></DockPanel>
                <CheckBox Content="双击耳边调节音量" IsChecked="{Binding EdgeDoubleTapVolume}" IsEnabled="{Binding GestureControlsEnabled}" />
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="音质与连接">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel MaxWidth="900" HorizontalAlignment="Center" Margin="28,26,28,40">
            <TextBlock Text="音质与连接" FontSize="24" FontWeight="Bold" Margin="0,0,0,18" />
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="音质" Style="{StaticResource SectionTitle}" />
                <Grid Margin="0,0,0,12">
                  <Grid.ColumnDefinitions><ColumnDefinition Width="180" /><ColumnDefinition Width="*" /><ColumnDefinition Width="90" /></Grid.ColumnDefinitions>
                  <TextBlock Text="左右声音平衡" Style="{StaticResource FieldLabel}" />
                  <Slider Grid.Column="1" Minimum="0" Maximum="32" TickFrequency="1" IsSnapToTickEnabled="True" Value="{Binding StereoBalance, Delay=400}" IsEnabled="{Binding CanControl}" />
                  <TextBlock Grid.Column="2" Text="{Binding StereoBalanceText}" HorizontalAlignment="Right" VerticalAlignment="Center" />
                </Grid>
                <CheckBox Content="360 音频" IsChecked="{Binding SpatialAudioEnabled}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="清晰通话" IsChecked="{Binding ExtraClearCallEnabled}" IsEnabled="{Binding CanControl}" />
                <TextBlock Text="360 音频仅切换耳机端状态；系统和内容仍需提供相应空间音频源" Style="{StaticResource VerificationText}" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="连接与通话" Style="{StaticResource SectionTitle}" />
                <CheckBox Content="无缝耳机连接" IsChecked="{Binding SeamlessConnection}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="摘下双耳时把通话切回电脑" IsChecked="{Binding CallPathControlEnabled}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="通话期间使用环境声" IsChecked="{Binding SidetoneEnabled}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="游戏模式" IsChecked="{Binding GamingModeEnabled}" IsEnabled="{Binding CanControl}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="摘下耳机时自动暂停" IsChecked="{Binding AutoPauseResumeEnabled}" IsEnabled="{Binding CanControl}" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="耳机回报状态" Style="{StaticResource SectionTitle}" />
                <Grid Margin="0,0,0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="Blade Light"/><TextBlock Grid.Column="1" Text="{Binding LightingControlText}"/></Grid>
                <Grid Margin="0,0,0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="免唤醒语音"/><TextBlock Grid.Column="1" Text="{Binding HotCommandText}"/></Grid>
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="Adapt Sound"/><TextBlock Grid.Column="1" Text="{Binding AdaptSoundText}"/></Grid>
                <TextBlock Text="只读展示；复合配置没有可靠写入格式" Style="{StaticResource VerificationText}" />
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="高级与测试">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel MaxWidth="900" HorizontalAlignment="Center" Margin="28,26,28,40">
            <TextBlock Text="高级与测试" FontSize="24" FontWeight="Bold" Margin="0,0,0,18" />
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="耳塞贴合度测试" Style="{StaticResource SectionTitle}" />
                <Grid Margin="0,0,0,6"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="左耳"/><TextBlock Grid.Column="1" Text="{Binding FitTestLeftText}"/></Grid>
                <Grid Margin="0,0,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="右耳"/><TextBlock Grid.Column="1" Text="{Binding FitTestRightText}"/></Grid>
                <Button Content="{Binding FitTestButtonText}" Style="{StaticResource PrimaryButton}" Command="{Binding ToggleFitTestCommand}" HorizontalAlignment="Left" />
                <TextBlock Text="双耳佩戴后开始；收到结果会自动停止" Style="{StaticResource VerificationText}" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="查找我的耳机" Style="{StaticResource SectionTitle}" />
                <Grid Margin="0,0,0,8"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="状态"/><TextBlock Grid.Column="1" Text="{Binding FindStatusText}"/></Grid>
                <CheckBox Content="左耳静音" IsChecked="{Binding FindLeftMuted}" IsEnabled="{Binding FindMuteEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="右耳静音" IsChecked="{Binding FindRightMuted}" IsEnabled="{Binding FindMuteEnabled}" Style="{StaticResource SettingCheck}" />
                <Button Content="{Binding FindButtonText}" Style="{StaticResource PrimaryButton}" IsEnabled="{Binding CanControl}" Click="FindButton_Click" HorizontalAlignment="Left" />
                <TextBlock Text="响铃可能很大声。开始前请确认耳机没有戴在耳朵里。" Foreground="{StaticResource WarningBrush}" Margin="0,10,0,0" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="实验功能" Style="{StaticResource SectionTitle}" />
                <CheckBox Content="自适应音量" IsChecked="{Binding AdaptiveVolumeEnabled}" IsEnabled="{Binding ExperimentalControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="警笛检测" IsChecked="{Binding SirenDetectEnabled}" IsEnabled="{Binding ExperimentalControlsEnabled}" />
                <TextBlock Text="默认禁用；当前只交叉确认了消息 ID" Style="{StaticResource VerificationText}" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="暂未开放" Style="{StaticResource SectionTitle}" />
                <TextBlock Text="Blade Light 写入  ·  等待参数语义" Margin="0,0,0,7" />
                <TextBlock Text="9 段自定义 EQ  ·  没有安全写入格式" Margin="0,0,0,7" />
                <TextBlock Text="固件更新  ·  不执行未验证刷写" />
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="验证中心">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
          <StackPanel MaxWidth="900" HorizontalAlignment="Center" Margin="28,26,28,40">
            <TextBlock Text="验证中心" FontSize="24" FontWeight="Bold" Margin="0,0,0,18" />
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="验证开关" Style="{StaticResource SectionTitle}" />
                <CheckBox Content="离线演示模式" IsChecked="{Binding DemoMode}" IsEnabled="{Binding ValidationControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="允许实验命令" IsChecked="{Binding ExperimentalCommands}" IsEnabled="{Binding ValidationControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <CheckBox Content="记住上次设置" IsChecked="{Binding RememberSettings}" IsEnabled="{Binding ValidationControlsEnabled}" Style="{StaticResource SettingCheck}" />
                <TextBlock Text="成功修改后保存；下次启动先显示记忆值，再由耳机扩展状态逐项校正。" Foreground="{StaticResource MutedBrush}" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="当前状态" Style="{StaticResource SectionTitle}" />
                <Grid Margin="0,0,0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="版本"/><TextBlock Grid.Column="1" Text="Windows 0.1.0"/></Grid>
                <Grid Margin="0,0,0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="控制链路"/><TextBlock Grid.Column="1" Text="{Binding ControlPath}"/></Grid>
                <Grid Margin="0,0,0,7"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="扩展状态"/><TextBlock Grid.Column="1" Text="{Binding ExtendedStateText}"/></Grid>
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/></Grid.ColumnDefinitions><TextBlock Text="命令记录"/><TextBlock Grid.Column="1" Text="{Binding CommandCount, StringFormat={}{0} 条}"/></Grid>
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="真机验证顺序" Style="{StaticResource SectionTitle}" />
                <TextBlock Text="1. 先验证连接、电量、降噪、环境声与六组 EQ" Margin="0,0,0,7" />
                <TextBlock Text="2. 再验证自适应、语音检测、触控锁、左右长捏与噪音循环" Margin="0,0,0,7" />
                <TextBlock Text="3. 最后验证平衡、通话、贴合度、查找与明确授权的实验功能" />
              </StackPanel>
            </Border>
            <Border Style="{StaticResource SectionBorder}">
              <StackPanel>
                <TextBlock Text="命令记录" Style="{StaticResource SectionTitle}" />
                <DataGrid ItemsSource="{Binding CommandLog}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column" Height="260" GridLinesVisibility="Horizontal" BorderBrush="{StaticResource LineBrush}">
                  <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding Timestamp, StringFormat=T}" Width="90" />
                    <DataGridTextColumn Header="结果" Binding="{Binding Result}" Width="90" />
                    <DataGridTextColumn Header="命令" Binding="{Binding Title}" Width="180" />
                    <DataGridTextColumn Header="数据包" Binding="{Binding Packet}" Width="220" />
                    <DataGridTextColumn Header="说明" Binding="{Binding Detail}" Width="*" />
                  </DataGrid.Columns>
                </DataGrid>
                <WrapPanel Margin="0,14,0,0">
                  <Button Content="复制记录" Command="{Binding CopyReportCommand}" />
                  <Button Content="保存为文件" Command="{Binding SaveReportCommand}" />
                  <Button Content="清空" Command="{Binding ClearLogCommand}" Margin="0" />
                </WrapPanel>
              </StackPanel>
            </Border>
          </StackPanel>
        </ScrollViewer>
      </TabItem>
    </TabControl>

    <Border Grid.Row="2" Background="White" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,1,0,0">
      <Grid Margin="24,0">
        <TextBlock Text="{Binding ConnectionDetail}" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" TextTrimming="CharacterEllipsis" />
        <TextBlock Text="非三星官方项目 · 真机验证版" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" FontSize="12" />
      </Grid>
    </Border>
  </Grid>
</Window>
